import React from 'react';
import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Dowon | Contact',
    description: 'This is the contact page of Kim Dowon, who records, learns, and develops.',
};

const ContactPage = () => {
    return <div>ContactPage입니다.</div>;
};

export default ContactPage;
